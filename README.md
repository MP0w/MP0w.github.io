# MPow

Personal site built with Jekyll and configured to run locally with Bundler.

The local setup is pinned to Jekyll `3.10` for compatibility with Ruby `2.6.x`
environments.

## Run locally

```bash
bundle install
bundle exec jekyll serve --livereload
```

Then open <http://127.0.0.1:4000>.
